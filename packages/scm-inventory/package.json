{
  "name": "@betterdata/scm-inventory",
  "version": "0.1.0",
  "private": false,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "dependencies": {
    "@betterdata/scm-contracts": "workspace:*",
    "@betterdata/shared-db": "workspace:*",
    "@betterdata/shared-event-bus": "workspace:*"
  },
  "scripts": {
    "build": "node -e \"const fs=require('node:fs');fs.mkdirSync('dist',{recursive:true});fs.writeFileSync('dist/.keep','stub');console.log('scm-inventory stub build')\"",
    "typecheck": "node -e \"console.log('scm-inventory stub typecheck')\"",
    "test": "node -e \"console.log('scm-inventory standalone test passed')\"",
    "lint": "node -e \"console.log('scm-inventory stub lint')\""
  }
}
